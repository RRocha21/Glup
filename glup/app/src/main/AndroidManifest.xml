<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.glup">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="GLUP"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Glup">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ParkingStartActivity" />
        <activity android:name=".Registration2Activity" />
        <activity android:name=".Forgot3Activity" />
        <activity android:name=".Forgot2Activity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".PaymentsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ForgotActivity" />
        <activity android:name=".ChangeActivity" />

    </application>

</manifest>